-- only works if model already created. which isn;t useful cos I need to define datatypes and constraint first
-- {{ codegen.generate_model_yaml(
--     model_names=['stg_sales__customers']
-- ) }}

-- {{ codegen.generate_source(
--     database_name='raw_data',
--     schema_name='raw_sales',
--     name='invdb'
-- ) }}
